# SRMS-Project
This C program is a role-based student management system that allows different types of users (admin, staff, guest) to log in and perform operations on student records stored in a text file
